# Automated Technical Documentation

> This document is automatically generated by the documentation pipeline.

## 1. FastAPI Router & Endpoints

| Endpoint Path | HTTP Method | Summary | Request Schema | Response Schema |
|---|---|---|---|---|
| `/api/v1/generate` | `POST` | Kick off the content pipeline and evaluate via Quality Gate. Registers job state before execution and updates it upon completion. | `ContentBriefRequest` | `ContentResponse` |
| `/api/v1/jobs` | `GET` | Retrieve list of all content generation jobs and their states. | `None` | `Dynamic` |
| `/api/v1/jobs/{job_id}` | `GET` | Retrieve details of a specific job by ID. | `None` | `Dynamic` |
| `/api/v1/docs` | `GET` | Retrieve the current project markdown documentation. | `None` | `Dynamic` |
| `/api/v1/docs/generate` | `POST` | Manually trigger regeneration of technical documentation. | `None` | `Dynamic` |
| `/api/v1/logs/stream` | `GET` | Stream live application logs in real-time via Server-Sent Events (SSE). | `None` | `Dynamic` |
| `/api/v1/health` | `GET` | No description | `None` | `Dynamic` |

## 2. Integrated Utility & Agent Tools

These tools are used for content research, formatting, and validation checks:

### `TavilySearchTool`

**Class Path:** `tools.TavilySearchTool`

**Description:**
Wrapper around the Tavily Search API.

    Usage:
        tool = TavilySearchTool()
        response = tool.search("latest trends in renewable energy")
        for result in response.results:
            print(result.title, result.url)

### `GrammarCheckTool`

**Class Path:** `tools.GrammarCheckTool`

**Description:**
Programmatic wrapper around the LanguageTool API.
    Used for direct queries and quality gate scoring.

### `CrewAIGrammarCheckTool`

**Class Path:** `tools.CrewAIGrammarCheckTool`

**Description:**
CrewAI agent tool wrapper. Exposes a text description and string-based report
    suitable for agent ingestion (e.g. Editor Agent).

### `SEOKeywordAnalyzer`

**Class Path:** `tools.SEOKeywordAnalyzer`

**Description:**
Programmatic analyzer for scoring article SEO metrics.

### `CrewAISEOTool`

**Class Path:** `tools.CrewAISEOTool`

**Description:**
CrewAI agent tool wrapper. Allows agents to run SEO checks and get recommendations.

### `PlagiarismDetector`

**Class Path:** `tools.PlagiarismDetector`

**Description:**
Programmatic plagiarism detector comparing text against reference documents
    semantically using SentenceTransformers.

### `CrewAIPlagiarismTool`

**Class Path:** `tools.CrewAIPlagiarismTool`

**Description:**
CrewAI agent tool wrapper. Exposes plagiarism checks to agents.

## 3. Automated Quality Gate System

The Quality Gate enforces strict content criteria before returning articles to the client:

- **Grammar Threshold:** Maximum of `5` errors allowed.
- **SEO Score Threshold:** Minimum of `70.0/100` score required.
- **Plagiarism Threshold:** Maximum of `15.0%` duplicate content allowed.

### Score Weightings

Quality score computation is dynamically weighted based on check inputs:

| Evaluation State | Grammar Weight | SEO Weight | Plagiarism Weight |
|---|---|---|---|
| **With Reference Docs** (Full Plagiarism Check) | 30% | 40% | 30% |
| **Without Reference Docs** (Plagiarism Check Skipped) | 40% | 60% | 0% |
